from core.dal.base.sqlite import EntityBase


def drop_order():
    # tables = list(EntityBase.metadata.tables.values())
    #
    # assert set(drop_order) == set(EntityBase.metadata.tables.values()), 'You must include all tables for migration!'
    #
    # return tables
    return None
